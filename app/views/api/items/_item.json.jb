{
  id: item.id,
  name: item.name,
  price: item.price,
  # tax: item.tax,
  # total: item.total,
  # discounted?: item.is_discounted?,
  description: item.description,
  selected: false
  # supplier: item.supplier.name
}